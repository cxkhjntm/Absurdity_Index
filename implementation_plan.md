# ABSURDITY INDEX // 荒谬指数 — 实现计划

## 项目概述

构建一个**赛博朋克风格的个人荒谬事件追踪系统**，将职场中遇到的荒谬流程/规则游戏化，通过积分、成就、等级、趋势图和 AI 生成的内容，让"吐槽"变成一种有仪式感的行为。

### 核心特性
- 🎮 游戏化积分系统（四级荒谬事件分类）
- 🤖 AI 深度集成（事件识别、成就生成、等级命名）
- 📊 数据可视化（趋势图、饼图、等级进度条）
- 📋 一键生成荒谬周报并导出为图片
- 🌃 暗黑赛博朋克视觉风格

---

## 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 结构 | HTML5 | 语义化标签，单页应用 |
| 样式 | Vanilla CSS | 赛博朋克主题、霓虹动画、故障风特效 |
| 逻辑 | Vanilla JavaScript (ES6+) | 模块化设计 |
| 数据 | localStorage | 浏览器本地持久化 |
| 图表 | [Chart.js](https://cdn.jsdelivr.net/npm/chart.js) | 折线图 + 环形图 |
| 导出 | [html2canvas](https://cdn.jsdelivr.net/npm/html2canvas) | 周报导出为图片 |
| AI | OpenAI 兼容 API | 用户自定义 baseURL + API Key |

> [!NOTE]
> Chart.js 和 html2canvas 通过 CDN 引入，无需构建工具。

---

## 页面模块设计

单页应用，通过侧边栏导航切换 5 个模块：

### 1. Dashboard // 仪表盘（首页）

**核心元素：**
- **荒谬总值**：大号霓虹数字，实时显示累计荒谬值
- **当前等级**：AI 生成的等级名称 + 进度条（距离下一阶段）
- **本周趋势**：迷你折线图预览
- **最近事件**：最近 5 条荒谬事件卡片
- **快速记录按钮**：顶部醒目的 `[+ RECORD // 记录荒谬]` 按钮

### 2. Record // 记录事件

**两种记录方式：**

**方式一：AI 智能识别**
- 文本输入框，用户自由描述发生的荒谬事件
- 点击"AI 分析"按钮，调用 AI 自动：
  - 识别荒谬事件等级（基础/连击/稀有/史诗）
  - 计算荒谬值
  - 生成对应成就/称号
- 用户确认后提交

**方式二：经典事件快捷按钮**
- 预设常见荒谬事件按钮，一键记录：
  - 🔄 "明明可以自动却非要手动"（+1）
  - 📝 "日报时间大于工作时间"（+1）
  - 📋 "同一字段三个地方手工填写"（+3）
  - 🔁 "重复填写已有系统里的数据"（+1）
  - 📊 "为了汇报而汇报"（+1）
  - 🙄 "领导也不知道为什么"（+10）
  - ❌ "优化方案被'上级要求'驳回"（+50）
- 点击后同样调用 AI 生成成就（如已配置 API）

### 3. Achievements // 成就殿堂

- 已解锁成就展示墙（卡片式布局，带发光特效）
- 未解锁成就显示为暗色/锁定状态
- 每个成就卡片包含：
  - 成就图标（emoji）
  - 成就名称（AI 生成）
  - 触发条件描述
  - 解锁时间
  - 稀有度标记（普通/稀有/史诗/传说）

### 4. Weekly Report // 荒谬周报

- 自动统计本周数据：
  - 本周总荒谬值 & 与上周对比
  - 荒谬值周趋势折线图
  - 各等级事件占比环形图
  - 最荒谬事件 TOP3
  - 本周新解锁成就
- **一键导出为图片**（html2canvas）
- 历史周报列表可回顾

### 5. Settings // 控制台

- **AI 配置面板：**
  - API Base URL 输入（兼容 OpenAI 格式）
  - API Key 输入（密码输入框）
  - 模型名称选择/输入
  - 连接测试按钮
- **提示词预设管理：**
  - 四类事件的成就生成提示词模板
  - 等级名称生成提示词模板
  - 支持编辑自定义
- **数据管理：**
  - 导出所有数据（JSON）
  - 导入数据
  - 清空数据（二次确认）

---

## 积分与等级系统

### 事件等级

| 等级 | 荒谬值 | 颜色标记 | 图标 | 典型场景 |
|------|--------|----------|------|----------|
| 基础 BASIC | +1 | 毒液绿 `#39FF14` | ⚡ | 明明可以自动却非要手动 |
| 连击 COMBO | +3 | 电磁蓝 `#00D4FF` | 🔗 | 同一字段多处重复填写 |
| 稀有 RARE | +10 | 电磁紫 `#BF40BF` | 💎 | 领导亲自吐槽流程 |
| 史诗 EPIC | +50 | 熔岩金 `#FFD700` | 🔥 | 优化被"上级要求"驳回 |

### 等级阶段

| 阶段 | 累计荒谬值范围 | 默认等级名（首次） |
|------|---------------|-------------------|
| 阶段 1 | 0 – 50 | 形式主义实习生 |
| 阶段 2 | 51 – 200 | 流程卷王 |
| 阶段 3 | 201 – 500 | 官僚主义大师 |
| 阶段 4 | 500+ | 荒谬觉醒者 |

### 等级名称动态变化规则

- 每次增加荒谬值时，有概率触发 AI 重新生成当前阶段的等级名称
- **变化概率** = `本次增加分数 / 距离下一阶段所需分数`
- 阶段 4（最终阶段）：固定 60% 概率变化
- AI 生成新名称时，需保持与当前阶段主题相符的幽默风格

---

## AI 集成设计

### API 调用架构

```
用户输入 → 构建 Prompt → fetch(baseURL + '/chat/completions') → 解析响应 → 更新 UI
```

### Prompt 模板（预设，可自定义）

**事件识别 Prompt：**
```
你是一个荒谬事件分析专家。用户会描述一个职场中遇到的荒谬事件。
请分析并返回 JSON 格式：
{
  "level": "basic|combo|rare|epic",
  "score": 数字,
  "title": "事件标题（简洁幽默）",
  "achievement": "成就名称（如果值得一个成就的话）",
  "achievement_desc": "成就描述",
  "comment": "一句毒舌点评"
}
```

**成就生成 Prompt：**
```
你是一个成就系统设计师，风格是赛博朋克+黑色幽默。
根据以下荒谬事件，生成一个有趣的成就：
事件等级：{level}，事件描述：{description}
返回 JSON：{ "name": "成就名称", "desc": "成就描述", "emoji": "一个合适的emoji" }
```

**等级名称生成 Prompt：**
```
你是一个赛博朋克世界的命名大师。
当前用户荒谬积分：{score}，所处阶段：{stage}（共4阶段）。
请生成一个有趣的、带有讽刺意味的等级称号，风格参考：形式主义实习生、流程卷王、官僚主义大师。
只返回称号本身，不要其他内容。
```

### AI 降级策略

当 AI 不可用时（未配置/请求失败）：
- 事件记录：使用预设的等级判断（用户手动选择等级）
- 成就生成：从预设成就池中随机选取
- 等级名称：使用预设名称列表

---

## 视觉设计规范

### 调色板

| 用途 | 色值 | 名称 |
|------|------|------|
| 主背景 | `#0a0a0f` | 深空黑 |
| 次背景 | `#12121a` | 暗夜蓝黑 |
| 卡片背景 | `#1a1a2e` | 星空灰 |
| 主强调色 | `#39FF14` | 毒液绿（霓虹） |
| 次强调色 | `#BF40BF` | 电磁紫 |
| 辅助色 | `#00D4FF` | 赛博蓝 |
| 警告/史诗 | `#FFD700` | 熔岩金 |
| 文字主色 | `#e0e0e0` | 银灰 |
| 文字次色 | `#888888` | 暗灰 |

### 特效

- **霓虹发光**：`text-shadow` / `box-shadow` 多层叠加
- **故障风（Glitch）**：CSS `@keyframes` 实现文字/元素抖动+色差
- **扫描线**：半透明横线 overlay，模拟 CRT 显示器
- **渐变边框**：`border-image` 或 `background-clip` 实现霓虹渐变边框
- **呼吸灯**：数值/重要元素的脉冲发光动画

### 字体

- 标题/数字：`'Orbitron'`（Google Fonts，科技感）
- 正文：`'Noto Sans SC'`（Google Fonts，中文）
- 代码/标签：`'JetBrains Mono'` 或 `monospace`

---

## 数据模型（localStorage）

```javascript
// 存储 key: 'absurdity_data'
{
  // 事件记录
  events: [
    {
      id: "uuid",
      timestamp: "ISO-8601",
      level: "basic|combo|rare|epic",
      score: 1,
      title: "事件标题",
      description: "用户描述",
      achievement: { name: "成就名", desc: "描述", emoji: "🎯" } | null,
      aiComment: "AI 毒舌点评" | null
    }
  ],

  // 成就列表
  achievements: [
    {
      id: "uuid",
      name: "成就名称",
      description: "成就描述",
      emoji: "🏆",
      rarity: "common|rare|epic|legendary",
      unlockedAt: "ISO-8601",
      eventId: "关联事件ID"
    }
  ],

  // 等级信息
  level: {
    stage: 1,           // 当前阶段 1-4
    totalScore: 0,      // 累计总分
    currentTitle: "形式主义实习生",  // 当前等级名称
    titleHistory: [     // 等级名称变更历史
      { title: "xxx", changedAt: "ISO-8601" }
    ]
  },

  // 周报历史
  weeklyReports: [
    {
      weekStart: "ISO-8601",
      weekEnd: "ISO-8601",
      totalScore: 0,
      eventCount: 0,
      topEvents: [],
      newAchievements: []
    }
  ],

  // AI 配置
  aiConfig: {
    baseUrl: "",
    apiKey: "",
    model: "gpt-3.5-turbo",
    prompts: {
      eventAnalysis: "...",
      achievementGen: "...",
      levelNaming: "..."
    }
  }
}
```

---

## 文件结构

```
荒谬指数/
├── index.html          # 主 HTML 文件，包含所有页面模块结构
├── css/
│   ├── index.css       # 设计系统：变量、重置、基础样式
│   ├── layout.css      # 侧边栏、页面容器布局
│   ├── components.css  # 卡片、按钮、输入框、模态框等组件
│   ├── dashboard.css   # 仪表盘模块样式
│   ├── record.css      # 记录事件模块样式
│   ├── achievements.css # 成就殿堂模块样式
│   ├── report.css      # 周报模块样式
│   ├── settings.css    # 设置模块样式
│   └── animations.css  # 霓虹、故障风、扫描线等动画
├── js/
│   ├── app.js          # 应用入口，路由/导航管理
│   ├── store.js        # 数据层：localStorage 读写、数据模型
│   ├── ai.js           # AI 服务层：API 调用、Prompt 构建、降级处理
│   ├── dashboard.js    # 仪表盘模块逻辑
│   ├── record.js       # 事件记录模块逻辑
│   ├── achievements.js # 成就模块逻辑
│   ├── report.js       # 周报生成与导出逻辑
│   ├── settings.js     # 设置模块逻辑
│   ├── charts.js       # 图表封装（Chart.js 配置）
│   └── utils.js        # 工具函数（UUID、日期等）
└── assets/
    └── favicon.svg     # 站点图标
```

---

## 实现阶段

### Phase 1: 基础框架 & 设计系统
1. 创建文件结构
2. 实现 CSS 设计系统（变量、主题、动画）
3. 构建 HTML 骨架（侧边栏 + 5 个模块容器）
4. 实现导航切换逻辑

### Phase 2: 数据层 & 核心功能
5. 实现 `store.js` 数据管理
6. 实现快捷事件按钮记录功能
7. 实现积分计算和等级系统
8. 实现仪表盘数据展示

### Phase 3: AI 集成
9. 实现 `ai.js` AI 服务层
10. 实现 Settings 页面 AI 配置
11. 实现 AI 事件分析功能
12. 实现 AI 成就生成
13. 实现 AI 等级命名

### Phase 4: 可视化 & 周报
14. 实现 Chart.js 图表（折线图 + 环形图）
15. 实现等级进度条
16. 实现周报生成页面
17. 实现周报导出为图片

### Phase 5: 打磨 & 优化
18. 完善动画和交互细节
19. 数据导入/导出功能
20. 响应式适配
21. 边界情况处理与错误提示

---

## 验证计划

### 手动验证
- 打开 `index.html`，验证所有页面模块切换正常
- 测试快捷按钮记录事件，验证积分和等级更新
- 配置 AI API，测试事件识别和成就生成
- 生成周报并导出为图片
- 验证 localStorage 数据持久化（刷新页面数据不丢失）
- 测试 AI 不可用时的降级策略
- 验证故障风/霓虹动画效果在主流浏览器正常

### 浏览器兼容
- Chrome（主要目标）
- Firefox
- Edge

---

## Open Questions

> [!IMPORTANT]
> **周报的时间范围**：周报统计的"一周"是自然周（周一到周日）还是从第一次记录开始每 7 天？建议使用自然周。

> [!NOTE]
> **预设快捷事件数量**：目前预设了 7 个经典荒谬事件按钮，后续你是否希望支持用户自定义新的快捷按钮？

> [!NOTE]
> **AI 生成的成就是否可以手动编辑**：AI 生成的成就名称/描述，提交前用户是否可以修改？建议允许修改以增加控制感。
